#include <iostream>
#include <vector>
using namespace std;
int main(int, char **)
{
    std::cout << "Hello, world!\n";
    // vector<pair<double, double>> grades;
    // grades.push_back({2, 83});
    // grades.push_back({1, 98});
    // grades.push_back({3, 87});
    // grades.push_back({2, 87});
    // grades.push_back({2, 83});
    // grades.push_back({3, 74});
    // grades.push_back({2, 95});
    // grades.push_back({1, 92});
    // double sum_grades = 0;
    // double points_sum = 0;
    // for(int i = 0; i < grades.size(); i++){
    //     sum_grades += grades[i].second * grades[i].first;
    //     points_sum += grades[i].first;
    // }
    // double res = sum_grades / points_sum;
    // cout << res * 0.700 << endl;
}